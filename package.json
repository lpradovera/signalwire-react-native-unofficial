{
  "name": "signalwire-react-native-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "example",
    "server"
  ],
  "scripts": {
    "build": "npm run build -w @signalwire/react-native",
    "test": "npm run test -w @signalwire/react-native && npm run test -w @signalwire/rn-push-server",
    "type-check": "npm run type-check -w @signalwire/react-native && npm run type-check -w @signalwire/react-native-example && npm run type-check -w @signalwire/rn-push-server",
    "lint": "eslint packages/react-native/src",
    "bundle-check": "npm run bundle-check -w @signalwire/react-native-example",
    "verify:prebuild": "node scripts/verify-prebuild.mjs",
    "verify:package": "npm run build && cd packages/react-native && npx publint && npx @arethetypeswrong/cli --pack .",
    "verify": "npm run lint && npm run type-check && npm test && npm run build && npm run bundle-check"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.19.0"
  }
}
