{
  "name": "@signalwire/rn-push-server",
  "version": "0.1.0",
  "private": true,
  "description": "Support server: device token registry and VoIP/FCM push sender for @signalwire/react-native",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "node --import tsx --watch --env-file-if-exists=.env src/index.ts",
    "start": "node --env-file-if-exists=.env dist/index.js",
    "build": "tsc",
    "test": "node --import tsx --test $(find src -name '*.test.ts')",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
